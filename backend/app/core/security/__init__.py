"""Security primitives: JWT, password hashing, secure headers.

These are *primitives* only — no auth flows live here. The ``auth`` module
(Phase F2) composes login/refresh/logout on top of them.
"""
