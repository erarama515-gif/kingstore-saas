# pytest config is sourced from pyproject.toml [tool.pytest.ini_options].
# This file is kept as a marker so plain `pytest` from backend/ works in editors that
# don't recognize pyproject-based pytest config.
[pytest]
addopts = -ra --strict-markers
testpaths = tests
