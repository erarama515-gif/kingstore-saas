# =============================================================================
# Kingstore SaaS — backend pinned dependencies
# Python: 3.12
# Lock with: pip-compile (future). For now, pin direct deps explicitly.
# =============================================================================

# --- Web framework ---
Flask==3.0.3
Werkzeug==3.0.4
gunicorn==23.0.0

# --- Persistence ---
SQLAlchemy==2.0.35
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
alembic==1.13.3
psycopg2-binary==2.9.9

# --- Auth & security ---
Flask-JWT-Extended==4.6.0
argon2-cffi==23.1.0
PyJWT==2.9.0

# --- Cross-cutting middleware ---
Flask-Cors==5.0.0
Flask-Limiter==3.8.0

# --- Async / cache ---
redis==5.0.8
celery==5.4.0

# --- Validation & serialization ---
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0

# --- Config / env ---
python-dotenv==1.0.1

# --- Logging ---
structlog==24.4.0
python-json-logger==2.0.7

# --- Dev / test (kept in same file for simplicity; split later) ---
pytest==8.3.3
pytest-flask==1.3.0
pytest-cov==5.0.0
factory-boy==3.3.1
ruff==0.6.9
mypy==1.11.2
